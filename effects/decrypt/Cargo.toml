[package]
name = "xclock-effect-decrypt"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Decrypt reveal effect for xclock."
repository = "https://github.com/xclock-cli/effects"

[[bin]]
name = "decrypt"
path = "src/main.rs"

[dependencies]
xclock-effects-lib = { path = "../effects-lib" }
